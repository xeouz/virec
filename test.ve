extern puti(n: int);

func pow(d:double, n:int): double
{
    let x=1.0d;

    for(let i=1; i<=n; ++i)
    {
        x*=d;
    }

    return x;
}

func is_munch(a: int): bool
{
    let sum=0.0d;

    let copy=a;
    while(copy>0)
    {
        let digit=copy%10;
        sum+=pow(digit,digit);
        copy/=10;
    }

    if(sum==a)
    {
        return true;
    }

    return false;
}

func main(): int
{
    for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }
    for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }
    for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }for(let i=1; i<1; ++i)
    {
        if(is_munch(i))
        {
            puti(i);
        }
    }
}