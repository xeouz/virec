extern puti(i:int)     returns int;
extern putd(i:double)  returns int;
extern putb(i:bool)    returns int;
extern put(c:char)     returns int;

func test() returns int
{
    return 1;
}

func main() returns int
{
    let a=10;

    puti(a);

    return 0;
}
