extern puti(i:int) returns int;

struct outer
{
    struct inner
    {
        int a;
        struct inner
        {
            int x;
        }
    }
    int b;
}

func main() returns int
{
    let a:outer;
    let b:outer;

    b.inner.inner.x=1201201;
    a=b;

    puti(a.inner.inner.x);

    return 0;
}
