extern putd(d:int)  returns int;
extern put(c:char)  returns int;

func main() returns int 
{
    for(let i=0; i<10; ++i)
    {
        for(let j=0; j<i; ++j)
        {
            put('#');
        }

        let nline:char = 10;
        put(nline);
    }
}