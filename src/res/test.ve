extern puti(n: int);

func sort(arr: int[5])
{
    let i=0;
    let key=0;
    let j=0;

    for(i=0; i<5; ++i)
    {
        key = arr[i];
        j = i-1;

        while(j>=0)
        {
            if(arr[j] < key)
            {
                break;
            }

            arr[j + 1] = arr[j];
            j-=1;
        }

        arr[j + 1] = key;
    }
}

let arr=[61,86,53,19,61];

sort(arr);

for(let i=0; i<5000000; ++i)
{
    let rr=[1,2,3,4,444];
    puti(arr[i]);
}