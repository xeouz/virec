extern puti(n:int) returns void;

func foo(n:int) returns int
{
    puti(n);

    if(n>0)
    {
        return foo(n-1);
    }
    
    return 0;
}

func main() returns int
{
    foo(10);
    return 10;
}