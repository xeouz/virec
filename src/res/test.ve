extern puti(n: int);

struct xyz
{
    int a;
    int b;
}

func test(a:xyz, b:xyz, something:bool): xyz
{
    if(something)
    {
        return a;
    }
    return b;
}
let a:xyz;
let b:xyz;

a.b=100;
b.b=200;

let c=test(a,b,true);

puti(c.b);