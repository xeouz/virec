extern putd(d:int) returns int;

func test() returns int
{
    let a=10;
    let i=0;
    for(i=0; i<100; i++)
    {
        a=a+1;
    }
    return a;
}

func main() returns int
{
    let a=test();

    putd(a);

    return a;
}